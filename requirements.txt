# Stock Tracker — Python Dependencies
# Install with: pip install -r requirements.txt

# HTTP requests
requests

# HTML parsing (uses Python's built-in html.parser — no C compiler needed)
beautifulsoup4

# YAML config
PyYAML

# Headless browser (karzanddolls + keraladiecastcars scrapers)
# After pip install, also run: playwright install chromium
playwright
